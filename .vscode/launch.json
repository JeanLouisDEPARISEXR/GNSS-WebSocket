{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run GNSS WebSocket Server",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/gnss_ws_server.py",
      "args": ["--port", "COM27", "--baud", "115200", "--host", "127.0.0.1", "--ws-port", "8765"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
